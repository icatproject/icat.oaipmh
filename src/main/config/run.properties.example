# Real comments in this file are marked with '#' whereas commented out lines
# are marked with '!'

# An example property (required to install the component)
required_property = example value

# The message to be returned (optional)
!message = this is an example message
